.post-card
  .post-header
    .post-title= post.title
    .post-actions
      = link_to "✏️", edit_board_post_path(@board, post), class: "post-action-icon", title: "Edit"
      = button_to "🗑️", board_post_path(@board, post), method: :delete, class: "post-action-icon", title: "Delete", data: {confirm: "Delete this post?"}
  .post-meta= time_ago_in_words(post.created_at) + " ago"
  .post-image= image_tag("/posts/image/#{post.id}?v=#{post.updated_at.to_i}")
  - if post.description.present?
    .post-description= post.description